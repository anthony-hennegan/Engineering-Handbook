# Python User Inputs

## Purpose

The `input()` function allows a Python program to receive information from the user.

When Python reaches an `input()` statement, the program pauses, displays a prompt, waits for the user to type a response, and stores the response as a string.

User input makes a program interactive.